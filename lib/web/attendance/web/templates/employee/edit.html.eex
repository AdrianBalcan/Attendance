<h2>Modificare date angajat</h2>

<%= render "form.html", changeset: @changeset,
                        companies: @companies,
                        action: employee_path(@conn, :update, @employee) %>

<%= link "Back", to: employee_path(@conn, :index) %>
