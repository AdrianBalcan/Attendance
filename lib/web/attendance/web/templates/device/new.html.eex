<h2>New device</h2>

<%= render "form.html", changeset: @changeset,
                        devicegroups: @devicegroups,
                        action: device_path(@conn, :create) %>

<%= link "Back", to: device_path(@conn, :index) %>
